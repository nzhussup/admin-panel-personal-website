export const floatingEmojisConfig = {
  // List of emojis to use in the background
  emojis: ["✨", "🌟", "💫", "⭐️", "🌠", "🚀", "🛸", "🪐", "🌍", "🌙"],
  // Number of emoji instances to create
  count: 50,
  // Animation duration range in seconds (min and max)
  durationRange: {
    min: 10,
    max: 20,
  },
  // Size range for emojis in pixels
  sizeRange: {
    min: 20,
    max: 40,
  },
  // Z-index for the background container
  zIndex: -1,
  // Opacity range for emojis
  opacityRange: {
    min: 0.3,
    max: 0.7,
  },
};
