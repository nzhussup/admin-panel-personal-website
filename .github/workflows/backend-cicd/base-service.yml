name: Base Service CI/CD

on:
  push:
    paths:
      - "backend/base-service/**"

jobs:
  call-service-cicd:
    uses: ./.github/workflows/common/backend-cicd.yml
    with:
      service_name: base-service
      service_path: backend/base-service
