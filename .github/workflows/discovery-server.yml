name: Discovery Server CI/CD

on:
  push:
    paths:
      - "backend/discovery-server/**"

jobs:
  call-service-cicd:
    uses: .github/workflows/backend-cicd-template.yml
    with:
      service_name: discovery-server
      service_path: backend/discovery-server
      lang: java
